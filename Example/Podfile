use_frameworks!

platform :ios, '12.0'

target 'Clavier_Example' do
  pod 'Clavier', :path => '../'

  target 'Clavier_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 5.0.1'
    pod 'Nimble', '~> 10.0.0'
  end
end
